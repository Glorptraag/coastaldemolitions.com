- forms: `contact-us/index.html` icon list (`.elementor-element-400884d`, the `<ul class="elementor-icon-list-items">` ~line of `13/48 Hutchinson St`) — the invoice reference block also carries ABN 72 650 242 158, the trading entity and office hours; there are no such items in the markup, so the kv table stops at Phone / Email / Office. Add `<li>` rows for ABN and Hours (CSS labels them from the `:has()` rules in `restyle/parts/forms.css`; a generic row gets the "Office" label, so a new row needs a distinguishing class or href).
- forms: `contact-us-ty/index.html` — the thank-you card has no "Back to home" link to style as the navy outlined button (the only link is the header wordmark). Add `<a class="btn is-secondary" href="/">Back to home</a>` after the `elementor-element-5856944` heading widget.
- forms: `contact-us-ty/index.html` repeats the full enquiry form (`.elementor-element-7f18ada`) under the thank-you card; it is kept and styled as "Another enquiry". If a second submit is not wanted on the thank-you page, remove the widget rather than hiding it.
- forms: `contact-us-ty/index.html` icon list says "58 Kortum Dr, Burleigh Heads QLD 4220" while `contact-us/index.html` says "13/48 Hutchinson St, Burleigh Heads QLD 4220" (the invoice address). Copy question, not CSS.
- forms: `404.html` line 12 (`h1{font-size:3rem…}`) — the page has no `<img>` wordmark; `forms.css` paints `/restyle/img/logo-wordmark-white.png` as a background on `body::before`, so it is invisible to assistive tech. If the brand should be announced, add `<img src="/restyle/img/logo-wordmark-white.png" alt="Coastal Demolitions">` inside a `<header>` before `<main>`.
- forms/base conflict: `restyle/parts/base.css` §5 reserves terracotta for the header phone stamp (chrome.css) and stamps every `.wpforms-submit` navy with `!important`; this brief makes the form submit the ONE terracotta action on /contact-us/ and /contact-us-ty/, so `forms.css` re-declares it terra `!important`. On those two pages the header phone stamp should be quietened to navy by the chrome owner (`body.page-id-194`, `body.page-id-10048`) so there is one terra action per view.
- `index.html` (and every page — header post 56, `header#header` desktop section `d2ba379`): there is no CTA button widget in the desktop header; `elementor-element-c0085d0` is an empty HTML widget. chrome.css uses the phone menu item `li.menu-item-9002 > a` as the terra stamp. If Dan wants "Get a quote" as the header action instead, add an `elementor-widget-button` after the menu widget in `d2ba379` (column `e1c2330`).
- `index.html` header band, right side: the option-L strap "What you see is what you get" (Poppins 500, `--bg300`) has no element to hang on. Add a text widget or `<span class="band-strap">` in column `e1c2330` before the menu; chrome.css will style `.band-strap`.
- `index.html` (and every page — footer post 68, `footer#footer`): section `elementor-element-4fe9974` is an empty boxed section. The option-L bar needs the entity line there: "Grunt Plant & Haulage Pty Ltd T/A Coastal Demolitions & Asbestos Removal · ABN 72 650 242 158 · © 2026". chrome.css already draws the 1px `--navy600` rule on `4fe9974` and styles any text inside it at 11px `--bg300`; wrap the ABN in `<span class="num">` for Plex Mono.
- Header/footer logo `<img>` (`wp-image-10975`, `width="482" height="209"`, mobile copy carries inline `style="padding-top:43.36%"`): chrome.css swaps the src to the white wordmark with `content:url()` and neutralises the padding with `!important`. Cleaner: point the header/footer img `data-lazy` at `/restyle/img/logo-wordmark-white.png`, set `width="501" height="168"`, and drop the inline padding.
- Verification note, not markup: `Chrome --headless=new --window-size=390,…` lays out at Chrome's 500px minimum window width and crops the PNG to 390, so every "phone" screenshot taken with the BRIEF.md command is a 500px layout. Check phone layouts in the Browser pane at a true 390 viewport (or old headless) before trusting a cut-off edge.
- home: `index.html:617-631` hero column `4af482b` carries only the H1 (`26649e0`), the Trustindex badge (`35e8187`) and the button (`8cab99f`). The option-L cover also has a Poppins 500 `--bg300` job line and a `--bg100` tag line under the title; there is no element for either, so home.css uses the badge as the second line. To match the quote cover add a text widget (or `<p class="job">…</p><p class="tag">What you see is what you get</p>`) between `26649e0` and `35e8187`; home.css will pick up `.job` / `.tag` inside `4af482b`.
- home/chrome conflict: home.css makes the hero "Get a Quote" (`8cab99f`) the ONE terracotta action on `/` (per its brief), re-declared `!important` over base.css §5. chrome.css stamps the header phone item terra on every page, so the homepage currently shows two. The chrome owner should quieten the header stamp to navy under `body.home` (as forms asked for `body.page-id-194` / `body.page-id-10048`).
- home: `index.html:928-971` section `6900294` (navy "Exceptional Mitigation…" band) only has content inside inner section `9a7b600`, which is `elementor-hidden-tablet elementor-hidden-mobile`; its twin `6172686` (`index.html:901-927`) is `elementor-hidden-desktop`. Below 1024px `6900294` was an empty 120px navy band whose stretched container also ran 15px past the phone viewport (pre-existing). home.css zeroes its padding ≤1024 and pins the container, but the section is dead weight there — cleaner to move the hidden-* classes onto `6900294` itself and drop them from `9a7b600`.
- home: `index.html:988-991` the pull quote ("Professional, reliable and prompt…") is an `<h3 class="elementor-heading-title">` inside section `159a95c`. It is styled as the ruled quote row, but it is a testimonial, not a heading: wrap as `<blockquote>` (or `<p>`) with a `<cite>` so the outline is not a fake H3 and the reviewer can be credited.
- home: `index.html:1012-1014` reviews shortcode `20b56fae` renders Trustindex layout `data-layout-id="14"` (slider) from cdn.trustindex.io, whose sheet rounds and colours the cards with `!important` at (0,5,0). home.css out-specifies it (`.ti-widget[data-layout-id] .ti-widget-container …`), which is brittle: if the widget layout or class names change the option-L squaring silently drops. A markup-free hardening is to switch the Trustindex layout to its "no-style/inherit" preset in the Trustindex admin.
- services: every service/location/about banner (e.g. `services/demolition/index.html` section `023614a`, `brisbane-demolitions/index.html` section `642f041`) has only the `<h1>`; there is no page-subtitle element for the option-L eyebrow, so `services.css` draws it with `h1::before{content:…}` ("Services" / "Service areas" / "About us" / "What we do" by body class). Generated text is invisible to assistive tech; if the eyebrow should be real, add `<p class="page-subtitle">Services</p>` before the H1 in each banner column and drop the `::before` rule.
- services: `services/asbestos-removal/index.html` section `cce806c` (and the other `cce806c` / `6349c27` / `d81f768` banners) carries an animated "scroll" mouse cue as an `elementor-widget-html` (`<div class="container_mouse">`). It has no place on the navy band, so `services.css` hides it (`display:none`); remove the widget if the preview is approved.
- services: `services/asbestos-removal/index.html` section `e0dc431` and `about-us/index.html` sections `a0e0f8a` / `9bd1e60` put body paragraphs inside a udesign heading widget (`<p class="title elementor-heading-title">…<br><br><b>1. Initial Consultation</b>…`). `services.css` restyles `p.title` as body copy, but the numbered process steps are one paragraph with `<br><br>` breaks; they should be a `<ol>` (or separate `<p>`s) in a text-editor widget so the square-marker list styling applies.
- services: `asbestos-removal-brisbane/index.html` (and the other 2025 location pages) write link colour inline — `<a style="color: #0000ff;">` and `<span style="color: #0000ff;">` around links — and `services/earthworks/index.html` section `d465785` writes `<p style="text-align: center;">`. `services.css` overrides both with `!important` (inline values); strip the inline styles when the markup is next touched.
- services: the same section hash means different things on different pages (`e0dc431` is the navy process band on `services/asbestos-removal/` but a light grey band on `brisbane-demolitions/`), so the navy-band text rules in `services.css` are enumerated per page-id + hash (36 pairs, generated from `wp-content/uploads/elementor/css/post-*.css`). Any new page that reuses a hash on a dark background needs adding to that list — or, cleaner, give dark sections a shared class (`on-navy`, which base.css already understands for buttons).
- services: `capability-statement/index.html` (page 11219) is a single hand-written `elementor-widget-html` with its own inline stylesheet, not the Elementor template; `services.css` only reaches its section rhythm. It already reads close to option L and was left as is.

## articles.css (guides, blog, projects) — owner: articles agent
- `projects/index.html:634` (and the other seven image boxes, e.g. `:645`): each tile caption is one string in `<p class="content">` ("1289 Gympie Road Aspley — Commercial Demolition"). The navy caption panel wants a `--bg300` micro label for suburb / type above the Poppins title; split it into `<small>Aspley · Commercial</small>` + the address (articles.css already styles `.image-box .title` / `.image-box-content small`).
- `asbestos-bathroom-removal-guide/index.html:704` (same block in all 46 guides): "Related Posts" is a 2-up Swiper (`data-slider-options … slidesPerView:2`, `slider-wrapper`). CSS can only skin the slides as licence cards; a static three-card grid needs the wrapper's `slider-wrapper` class and `data-slider-options` removed and `cols-lg-2` → `cols-lg-3`.
- `blog/index.html:658` (every card, and the archives): `.post-meta` carries `0 Like Post` (a `#` vote link) and `Comments Off`. They are muted to 13px `--ink3`; they should be deleted, they are theme noise, not content.
- `27-avanti-street-mermaid-beach/index.html:633` (`elementor-element-e0c72eb`, and its sibling on the other six Elementor project pages): the "GET A QUOTE" button is navy, because base.css §5 gives the one terra per view to the header phone stamp. If Drew wants the quote button to carry terra instead, add class `is-primary` to that widget div and quieten the header stamp on those page ids.
- `1289-gympie-road-aspley-commercial-demolition/index.html:615-628` and `8-kingfisher-court-burleigh-heads-residential-demolition/index.html` (same blocks): these two case studies are HTML widgets with inline `<style>` (Montserrat, `#c9553d`, 4-8px radii, drop shadows) and inline `style="color:#444"` on paragraphs. articles.css out-cascades them by order and uses `!important` on the inline-styled paragraphs; the clean fix is to drop those `<style>` blocks and inline attributes once the site CSS owns the look.
- `asbestos-bathroom-removal-guide/index.html:641` (guide meta line, all guides): the brief wanted "author · date · reading time" on the guide; the template only has date and category in three heading widgets. Author and reading time need adding to the template (author exists on the archive cards, not on the single).
